passo 1
yarn (para baixar as dependencias.)

No arquivo package.json tem a instrução
"scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
com isso tem a instrução para rodar conforme o comando abaixo
yarn dev
